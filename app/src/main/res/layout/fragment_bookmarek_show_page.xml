<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/findFragmentShowPage"
    tools:context=".Fragments.BookmarekShowPage">

   <androidx.recyclerview.widget.RecyclerView
       android:layout_width="match_parent"
       android:layout_height="match_parent"
       android:id="@+id/bookmarkRecyclerView"
       />
   <ImageView
       android:layout_width="50dp"
       android:layout_height="50dp"
       android:id="@+id/scrollToTop"
       android:visibility="invisible"
       android:layout_gravity="right|bottom"
       android:layout_marginBottom="100dp"
       />
   <LinearLayout
       android:layout_width="240dp"
       android:layout_height="240dp"
       android:id="@+id/loadingNoBookmark"
       android:layout_gravity="center"
       android:orientation="vertical"
       android:visibility="invisible"
       >
      <ImageView
       android:layout_width="250dp"
       android:layout_height="250dp"
       android:src="@drawable/bookmark_svg"
       android:id="@+id/showNoBookmark"
       android:layout_gravity="center"
       android:layout_weight="1"
       />
      <TextView
          android:layout_width="match_parent"
          android:layout_height="wrap_content"
          android:text="No Bookmark Added"
          android:textColor="@color/black"
          android:textSize="25sp"
          android:layout_marginTop="20dp"
          android:textStyle="bold"
       />
      <TextView
          android:layout_width="wrap_content"
          android:layout_height="wrap_content"
          android:text="Tap On Title to add Bookmark"
          android:layout_marginTop="10dp"
          android:textSize="14sp"
          android:textStyle="italic"
          android:textColor="@color/skyBlue"
          android:layout_gravity="center_horizontal"
         />

   </LinearLayout>

</FrameLayout>